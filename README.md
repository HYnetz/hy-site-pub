# Hy Setup Pack v2 — SeedNet→MoneyNet (additive only)
Additive-only configs + auto-setup. MoneyMode OFF by default.
Quick start:
  unzip hy_setup_pack_v2.zip -d hy && cd hy
  bash scripts/auto-setup.sh --profile seednet --domain yourdomain.tld
Flip later:
  bash scripts/flip-money-mode.sh on
